# TTM Forecasting API Configuration Example
# Copy this file to .env and update the values for your environment

# Data source configuration
# Base URL for your metrics API endpoint
DATA_BASE_URL=http://localhost:8080

# Request timeout in seconds for data fetching
REQUEST_TIMEOUT=30

# Authentication (optional)
# Bearer token for API authentication - leave empty if not needed
AUTH_TOKEN=your_bearer_token_here

# API Server configuration
# Host and port for the FastAPI server
API_HOST=0.0.0.0
API_PORT=8000
API_DEBUG=false

# Forecasting defaults
# Default number of days to forecast when not specified in requests
DEFAULT_FORECAST_HORIZON=30

# Whether to use enhanced TTM models by default
DEFAULT_USE_ENHANCED_TTM=false

# Plotting configuration (for API)
# Whether to save plots to disk during API calls
SAVE_PLOTS=false

# Whether to display plots during API calls (should be false for headless servers)
SHOW_PLOTS=false

# Example configurations for different environments:

# Development environment:
# DATA_BASE_URL=http://localhost:3000
# API_PORT=8000
# API_DEBUG=true

# Production environment:
# DATA_BASE_URL=https://metrics.yourcompany.com
# API_PORT=80
# API_DEBUG=false
# AUTH_TOKEN=prod_bearer_token_12345

# Docker environment:
# DATA_BASE_URL=http://metrics-service:8080
# API_HOST=0.0.0.0
# API_PORT=8000